
{{-- <table>
    @foreach ($file as $item)
        <tr>
            <td>{{ $item->file }}</td>
            <td>
                <a href="{{ asset('file_suratmasuk/' . $item->file) }}" class="btn btn-danger btn-sm ms-3" download>Download</a>
                <a href="{{ asset('file_suratmasuk/' . $item->file) }}" class="btn btn-primary btn-sm ms-3" target="_blank">View</a>
            </td>
        </tr>
    @endforeach
</table> --}}
